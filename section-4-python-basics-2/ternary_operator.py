############################################################
#                      Ternary Operator                    #
############################################################

""" 
The ternary operator is a shortcut for the if-else statement.
It can also be called a conditional expression. 
It's an expression because it returns a value.

"""

# condition_if_true if condition else condition_if_else

is_bububuddy = True
can_message = "message_allowed" if is_bububuddy else "message_not_allowed"

print(can_message) # message_allowed
